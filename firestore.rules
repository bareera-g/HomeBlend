rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode: allow read/write for development. Tighten for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
